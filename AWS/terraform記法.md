# terraform を操作する際の記法

# 各種ファイル
- `.tf` ファイルはすべて統合された状態で実行されるため、ファイル分割は管理しやすさのみ


# 各種コマンド
- resource
    - 新しいリソースを作成する
- data
    - 既存のリソースを参照する
- variable
    - 外部から値を注入する
    - `variable` で値を宣言し、`var.xxx` で参照する

